{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "bash cron/healthcheck.sh",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
